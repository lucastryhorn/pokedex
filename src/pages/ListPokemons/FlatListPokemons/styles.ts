import { FlatListProps } from 'react-native';
import styled from 'styled-components/native';

export const ListPokemons = styled.FlatList.attrs<FlatListProps<JSX.Element>>({
  contentContainerStyle: 10,
})``;
